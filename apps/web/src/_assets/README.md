# Shared font assets

`geist-variable.ttf` is Geist by Vercel, from the Google Fonts repository, distributed under the SIL Open Font License in `Geist-OFL.txt`. Self-hosted with `next/font/local` in the root layout as the site's only typeface (`--font-geist`).
